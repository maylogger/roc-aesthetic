Generate a 「最新消息」listing page（ROC agency / cram school vibe).

Requirements:
- table OR dual-column list with cramped date column (ROC calendar style optional mixed with Gregorian)
- category tags as inline colored cells or bordered pills with inconsistent rounding
- pagination as tiny text links, not slick component
- top yellow/red alert banner possible
- right sidebar 「熱門點閱」「相關法規」「快速連結」dump
- 「more」 links aggressively utilitarian

Avoid:
Card masonry, infinite-scroll startup feed.
